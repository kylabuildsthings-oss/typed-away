# Typed Away — browser app

Vite + React + TypeScript port of the Coding Dojo trainer.

```bash
npm install
npm run dev
```

Open http://localhost:5173/

Katas are loaded from `public/questions.json` (copied from the repo root `questions.json`).
